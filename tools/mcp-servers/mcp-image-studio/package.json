{
  "name": "mcp-image-studio",
  "version": "1.0.0",
  "description": "MCP server for AI image generation via AIML API",
  "type": "module",
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test --env-file=.env.test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest"
  }
}
